import type { ChartAppearance } from '../types/capability'
interface AppearanceControlsProps { appearance: ChartAppearance; onChange: (patch: Partial<ChartAppearance>) => void }
export function AppearanceControls({ appearance, onChange }: AppearanceControlsProps) { return <section className="panel-section appearance-section"><div className="section-heading"><span>Appearance</span><span className="section-kicker">SVG output</span></div><div className="color-grid">{([['fillColor', 'Fill'], ['outlineColor', 'Outline'], ['axisColor', 'Axes'], ['textColor', 'Text'], ['backgroundColor', 'Background']] as const).map(([key, label]) => <label key={key} className="color-field"><span>{label}</span><input type="color" value={appearance[key]} onChange={(event) => onChange({ [key]: event.target.value })} /></label>)}</div><label className="opacity-field"><span>Fill opacity <strong>{Math.round(appearance.fillOpacity * 100)}%</strong></span><input type="range" min="0.1" max="0.7" step="0.01" value={appearance.fillOpacity} onChange={(event) => onChange({ fillOpacity: Number(event.target.value) })} /></label><div className="toggle-grid"><label><input type="checkbox" checked={appearance.showRings} onChange={(event) => onChange({ showRings: event.target.checked })} /> Guide rings</label><label><input type="checkbox" checked={appearance.showPercentages} onChange={(event) => onChange({ showPercentages: event.target.checked })} /> Show percentages</label><label><input type="checkbox" checked={appearance.showTitle} onChange={(event) => onChange({ showTitle: event.target.checked })} /> Show title in SVG</label></div></section> }