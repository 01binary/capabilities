import type { CapabilitySkill } from '../types/capability'
interface SkillControlProps { skill: CapabilitySkill; onChange: (patch: Partial<CapabilitySkill>) => void }
export function SkillControl({ skill, onChange }: SkillControlProps) { return <div className="skill-control"><div className="skill-heading"><span className={`direction-dot direction-${skill.direction}`} /><input aria-label={`${skill.direction} capability label`} value={skill.label} onChange={(event) => onChange({ label: event.target.value })} /></div><div className="skill-input-row"><input aria-label={`${skill.label} percentage slider`} type="range" min="0" max="100" value={skill.value} onChange={(event) => onChange({ value: Number(event.target.value) })} /><input aria-label={`${skill.label} percentage`} className="number-input" type="number" min="0" max="100" value={skill.value} onChange={(event) => onChange({ value: Math.max(0, Math.min(100, Number(event.target.value) || 0)) })} /><span>%</span></div></div> }